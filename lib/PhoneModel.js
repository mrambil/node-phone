module.exports = {
  find(id) {
    return id;
    // NOP
  },

  attributes: {
    // e.g., "Polly"
    model: {
      type: 'string',
    },

    // e.g., 3.26
    brand: {
      type: 'string',
    },

  },

};
